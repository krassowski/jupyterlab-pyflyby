nodeLinker: node-modules

# Do not run `postinstall`/`preinstall`/`prepare` scripts of dependencies.
# Hardens against supply chain attacks that rely on install-time script execution.
enableScripts: false
